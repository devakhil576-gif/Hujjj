const input=document.getElementById('input');const chat=document.getElementById('chat');const send=document.getElementById('send');const status=document.getElementById('keyStatus');const modal=document.getElementById('settingsModal');const apiKey=document.getElementById('apiKey');let history=[];
function updateStatus(){const ready=window.HealthBridge&&HealthBridge.getApiKeyStatus()==='ready';status.textContent=ready?'● AI connected':'● AI key not configured';status.classList.toggle('ready',ready)}
function escapeHtml(s){return String(s).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}
function addMessage(role,text){const empty=document.getElementById('emptyState');if(empty)empty.remove();const el=document.createElement('div');el.className='message '+role;el.innerHTML='<div class="bubble">'+escapeHtml(text)+'</div>';chat.appendChild(el);chat.scrollTop=chat.scrollHeight}
function sendMessage(e){e.preventDefault();const text=input.value.trim();if(!text||send.disabled)return;if(!window.HealthBridge){addMessage('assistant','The native Android bridge is unavailable. Reopen the app.');return}history.push({role:'user',text});addMessage('user',text);input.value='';input.style.height='auto';send.disabled=true;const thinking=document.createElement('div');thinking.id='thinking';thinking.className='message assistant';thinking.innerHTML='<div class="bubble">Checking medical information…</div>';chat.appendChild(thinking);HealthBridge.ask(JSON.stringify(history))}
window.onHealthBotResult=function(ok,text){const thinking=document.getElementById('thinking');if(thinking)thinking.remove();send.disabled=false;if(ok){history.push({role:'assistant',text});addMessage('assistant',text)}else addMessage('assistant','I could not complete the consultation. '+text)};
function usePrompt(text){input.value=text;input.focus();input.style.height='auto';input.style.height=input.scrollHeight+'px'}
function clearChat(){history=[];chat.innerHTML='<div class="empty-state" id="emptyState"><div class="bot-orb">✚</div><h2>Medical Assistant</h2><p>Describe what is happening, how long it has been happening, and anything relevant such as age range, symptoms, or a clinician\'s instructions.</p><div class="chips"><button onclick="usePrompt(\'What could cause a mild headache, and what warning signs should I watch for?\')">Headache</button><button onclick="usePrompt(\'What are common causes of acne, and what general care is evidence-based?\')">Skin</button><button onclick="usePrompt(\'What information should I tell a doctor about a new symptom?\')">Doctor visit</button></div></div>';updateStatus()}
function openSettings(){modal.classList.add('open');modal.setAttribute('aria-hidden','false');apiKey.value=''}function closeSettings(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}
function saveKey(){const key=apiKey.value.trim();if(!key){addMessage('assistant','Please enter a Gemini API key.');return}HealthBridge.saveApiKey(key);closeSettings();updateStatus()}
function clearKey(){HealthBridge.clearApiKey();apiKey.value='';closeSettings();updateStatus()}
function saveSummary(){if(!history.length){addMessage('assistant','There is no consultation to save yet.');return}const text=history.map(m=>(m.role==='user'?'Patient inquiry':'HealthBot')+': '+m.text).join('\n\n');const blob=new Blob([text],{type:'text/plain'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='healthbot-consultation.txt';a.click();URL.revokeObjectURL(a.href)}
input.addEventListener('input',()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,140)+'px'});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();document.querySelector('.composer').requestSubmit()}});updateStatus();